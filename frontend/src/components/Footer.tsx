import React from 'react';

export default function Footer() {
  return (
    <div className="footer">
      <div className="container">© {new Date().getFullYear()} FashionAI — Built with care</div>
    </div>
  );
}
